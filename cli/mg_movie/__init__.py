"""Math Gallery movie CLI: render deep-zoom fractal dives to video.

Uses the shared native core (`mg_core`) so the math matches the browser viewer
exactly, then stitches frames with ffmpeg.
"""

__version__ = "0.1.0"
