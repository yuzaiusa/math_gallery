# Build artifacts
build/
dist/
*.egg-info/
__pycache__/
*.pyc

# Native modules
*.so
*.dylib
*.pyd

# CI-built WASM (never committed; built by the Pages workflow)
pages/mandelbrot/wasm/core.js
pages/mandelbrot/wasm/core.wasm

# Movie output (large renders stay out of git; YouTube-hosted)
*.mp4
*.mov
frames/
# ...except the small committed web preview loops for the film gallery
!assets/films/*.mp4

# Editor / OS
.DS_Store
.venv/
venv/
